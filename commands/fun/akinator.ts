import { Command } from '@/types';

export default () =>
  new Command({
    name: 'akinator',
    description: 'Play A Game Of Akinator',
    aliases: [],
    run: async (_, __, ___) => void 0
  });
